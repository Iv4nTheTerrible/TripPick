name: trip_pick_server
description: Local API proxy for the TripPick Flutter MVP.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.11.5

dependencies:
  http: ^1.6.0
  shelf: ^1.4.2

dev_dependencies:
  lints: ^6.1.0
  test: ^1.30.0
